# Auto-generated by meowapps dev — Shopify CLI requires this file at project root.
# Safe to delete; it will be recreated on next `meowapps dev`.

roles = ["frontend"]
port = 3000

[commands]
dev = "npx meowapps dev --only-web"
